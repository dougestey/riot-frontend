# Payload API origin (used by @payloadcms/sdk for both server and client).
# Use your backend URL for local, staging, or production (e.g. https://your-backend.example.com for staging).
NEXT_PUBLIC_API_URL=http://localhost:3000
